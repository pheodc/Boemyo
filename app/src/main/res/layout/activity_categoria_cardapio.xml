<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="br.com.boemyo.Activitys.CategoriaCardapioActivity">

    <include android:id="@+id/tb_categoria_cardapio"
        layout="@layout/toolbar"></include>

    <include android:id="@+id/conexao_categoria_cardapio"
        android:layout_height="40dp"
        android:layout_width="match_parent"
        android:layout_below="@id/tb_categoria_cardapio"
        layout="@layout/conexao"
        android:visibility="gone"
        ></include>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="8dp"
        android:divider="@null"
        android:layout_below="@+id/tb_categoria_cardapio"
        android:id="@+id/lv_categoria_cardapio"></ListView>
</RelativeLayout>
